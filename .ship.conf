# Generated by git-ship. See 'git-ship --man' for help or https://github.com/jhthorsen/app-git-ship
class = App::git::ship::perl
project_name =
homepage = https://github.com/kiwiroy/alien-libdeflate
bugtracker = https://github.com/kiwiroy/alien-libdeflate/issues
license = artistic_2
# build test happens before `before_build` need to have everything ready, which
# isn't really possible
build_test_options = # -b -r
before_build = cpanm --installdeps -n -q .
